{
  "name": "pinelines",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^6.1.4",
    "@testing-library/react": "^14.1.0",
    "@testing-library/user-event": "^13.5.0",
    "deploy-gh-pages": "^1.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "jest",
    "clear:cache": "yarn test --coverage --clearCache",
    "test:coverage": "yarn test --coverage",
    "eject": "react-scripts eject",
    "prepare-commit": "bash ./scripts/prepare-commit.sh",
    "predeploy": "yarn build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@babel/preset-env": "^7.23.3",
    "@babel/preset-react": "^7.23.3",
    "babel-jest": "^29.7.0",
    "husky": "^8.0.3",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "react-test-renderer": "^18.2.0"
  },
  "overrides": {
    "@svgr/webpack": "$@svgr/webpack"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn prepare-commit",
      "pre-push": "yarn prepare-commit"
    }
  },
  "homepage": " https://phamdatt.github.io/React-Test"
}
